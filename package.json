{
  "name": "@maximprotocol/program",
  "version": "0.1.0",
  "description": "Maxim Protocol Solana on-chain program — agent wallet registry, payment ledger, and spend policy enforcement",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "anchor build",
    "test": "anchor test",
    "deploy:devnet": "anchor deploy --provider.cluster devnet",
    "deploy:mainnet": "anchor deploy --provider.cluster mainnet",
    "keys:sync": "anchor keys sync",
    "idl:build": "anchor idl build"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.29.0",
    "@solana/spl-token": "^0.4.6",
    "@solana/web3.js": "^1.87.6"
  },
  "devDependencies": {
    "@types/chai": "^4.3.5",
    "@types/mocha": "^10.0.6",
    "chai": "^4.3.10",
    "mocha": "^10.3.0",
    "ts-mocha": "^10.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
